# Lower bounds rather than exact pins: the stack is validated on Python 3.11-3.14.
# Freeze with `pip freeze > requirements.lock.txt` when promoting to an environment.
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
SQLAlchemy>=2.0.36
argon2-cffi>=23.1
cryptography>=44.0
PyJWT>=2.10
pyotp>=2.9
qrcode>=8.0
python-docx>=1.1.2
# Reads the Google Forms response workbook (app/scripts/from_forms.py).
openpyxl>=3.1
python-multipart>=0.0.20
anthropic>=1.0,<2
httpx>=0.28
email-validator>=2.2

# PostgreSQL driver - required for the production DATABASE_URL, unused on SQLite.
psycopg[binary]>=3.2
